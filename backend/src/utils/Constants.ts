enum ModelsEnum {
  Admin = 'Admin',
  People = 'People',
  Company = 'Company',
}

enum Roles {
  admin = 'admin',
}

enum Resource {
  people = 'people',
  company = 'company',
}

export { Roles, ModelsEnum, Resource };
